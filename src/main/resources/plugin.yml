name: ${name}
version: ${project.version}
description: ${description}
website: ${url}
author: ${author}
main: ${mainClass}

prefix: Craftory
depend: [ItemsAdder]
loadafter: [ItemsAdder]

commands:
  toggleDebugMode:
    description: Toggle debug mode
    usage: /<command>

permissions: {}