name: ${name}
version: ${project.version}
description: ${description}
website: ${url}
author: ${author}
main: ${mainClass}

prefix: Craftory
depend: [ItemsAdder, Citizens]
loadafter: [ItemsAdder]

commands:
  matty:
    description: TEST
    usage: /<command>
  setCursedSpreadRate:
    description: Sets the rate that cursed earth spreads
    usage: /<command> rate(1.0 is default)
  toggleDebugMode:
    description: Toggle debug mode
    usage: /<command>

permissions: {}